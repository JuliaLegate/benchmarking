# Previous harness

The previous benchmarking scripts, results, and presentation assets are kept
here unchanged. They are archival; the maintained harness is at the repository
root. Their original history remains available before the migration commit.

The replacement harness was imported from JuliaLegate/cuNumeric.jl's
`benchmark/` directory at commit `88d82fe9` (plus standalone setup/container-CI
changes). NAS third-party attribution is in `../nas/THIRD_PARTY_LICENSE.md`.
